export enum AppointmentType {
  CONSULTATION = 'consultation', // aba "Consulta" do modal — exige paciente
  COMMITMENT = 'commitment', // aba "Compromisso" — bloqueio de agenda, reunião etc., sem paciente
}
